/* Resposivo Mobile */
@media (max-width: 768px) {

    .top-header {
        width: 100%;
        height: 52px !important;
        background-color: #005CFF;
    }

    .top-header p {
        padding: 4px 22px;
    }

    .header-container {
        height: 132px; 
        border-bottom: 1px solid rgba(0, 0, 0, 0.16);
        background-color: red;
    }

    .header {
        flex-direction: column;
        align-items: center; /* Centraliza os itens horizontalmente */
        position: relative;
        margin-top: 0; /* Remove a margem superior */
        background-color: #6220C1
    }

    .header .logo {
        width: 148px;
        height: 26px;
        text-align: center;
        margin-bottom: 27px; /* Adiciona espaço abaixo do logo */
        
    }

    .header .search-and-cart {
        display: flex;
        flex-direction: column; /* Empilha os elementos */
        align-items: center; /* Centraliza horizontalmente */
        position: relative;
        width: 100%;
    }

    .header .search-and-cart .search-box {
        margin-bottom: 20px; /* Espaço abaixo da barra de busca */
        width: calc(100% - 32px); /* Ocupa a largura com margens laterais */
        margin-left: 16px;
        margin-right: 16px;
        max-width: 294px; /* Define uma largura máxima para a busca */
    }

    .search-and-cart .shopping-cart {
        position: absolute;
        top: -102px;
       right: -110px;
     }

    .search-and-cart .shopping-cart .cart-badge {
        position: absolute;
        top: -6px;
        right: -6px;
    }


    .menu-hamburger {
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        margin-right: auto; /* Empurra o ícone para a esquerda */
        font-size: 24px;
        top: 62px;
        position: absolute;
        z-index: 10; /* Garante que esteja acima de outros elementos */
    }

    .departments.navbar-links,
    .menu-hamburger a#allCategoriesLink,
    .search-and-cart>a:nth-child(2),
    .search-and-cart .text-info-header {
        display: none !important;
    }
}